export const products = `{
    products {
      id,
      sku,
      product_name,
      featured_image,
      current_price,
      on_sale,
      sale_price,
      slug,
      categories
    }
  }`;
